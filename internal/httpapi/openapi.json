{
  "openapi": "3.1.0",
  "info": {
    "title": "hubbub",
    "version": "0.0.0-injected-at-serve-time",
    "summary": "Authenticated notification fan-out hub.",
    "description": "One JSON API in, delivery to the operator's channels out.\n\nCallers are machines, so every outcome is explicit enough to branch on: the HTTP status carries the verdict and `result` repeats it in the body. A `202` is a promise, not a receipt — the message is durably queued and its outcome is written to the hub's delivery log later, keyed by the `requestId` this call returned.\n\nAuthentication is a bearer key. The key's channel permission list *is* its delivery set: omit `channels` and the notification goes to every channel the key permits. Supplying `channels` narrows that set and can never widen it — naming a channel the key lacks is a `403`, not a silent intersection.\n\nDelivery is at-least-once: a caller that times out and retries may double-send."
  },
  "servers": [
    {
      "url": "/",
      "description": "Replaced at serve time with this deployment's base URL."
    }
  ],
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "paths": {
    "/v1/notify": {
      "post": {
        "operationId": "notify",
        "summary": "Send a notification to the caller's channels.",
        "description": "Enqueues the notification for every target channel, waits out the server's response window, and reports what is known by then: attempts that finished are reported concretely, anything still in flight as `queued`.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NotifyRequest"
              },
              "examples": {
                "minimal": {
                  "summary": "Title and message only — goes to every channel the key permits",
                  "value": {
                    "title": "Backup failed",
                    "message": "nightly rsync exited 1"
                  }
                },
                "withPriorityAndTags": {
                  "summary": "Priority and tags are display hints; neither changes routing",
                  "value": {
                    "title": "Disk almost full",
                    "message": "/ is at 94% on tools",
                    "priority": "urgent",
                    "tags": ["disk", "cron"]
                  }
                },
                "richEmail": {
                  "summary": "html adds a rich body for channels that render one; message stays the plain-text alternative",
                  "value": {
                    "title": "Weekly digest",
                    "message": "3 jobs failed, 41 succeeded. See the run log.",
                    "html": "<p>3 jobs failed, 41 succeeded.</p><table><tr><th>job</th><th>exit</th></tr><tr><td>borg</td><td>1</td></tr></table>",
                    "tags": ["digest"]
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Delivered: every target channel completed successfully inside the response window.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotifyResponse"
                },
                "example": {
                  "result": "delivered",
                  "requestId": "r_cc519c9a3ee79f270c4e9823cd56f2f3",
                  "channels": {
                    "ntfy": "ok"
                  }
                }
              }
            }
          },
          "202": {
            "description": "Queued: nothing finished inside the response window. The message is durably spooled and will be retried; the outcome lands in the delivery log under this `requestId`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotifyResponse"
                },
                "example": {
                  "result": "queued",
                  "requestId": "r_cc519c9a3ee79f270c4e9823cd56f2f3",
                  "channels": {
                    "ntfy": "queued"
                  }
                }
              }
            }
          },
          "207": {
            "description": "Partial: channels disagreed — some combination of delivered, queued, failed or disabled. Inspect `channels` per id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotifyResponse"
                },
                "example": {
                  "result": "partial",
                  "requestId": "r_cc519c9a3ee79f270c4e9823cd56f2f3",
                  "channels": {
                    "ntfy": "ok",
                    "parked": "disabled"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Malformed request: bad JSON, an unknown field, an unparseable priority, an over-length body, or `\"channels\": []` (omit the field instead — an empty list is a caller that computed no targets, and must not fan out to everything).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Missing or unknown bearer key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "403": {
            "description": "The request named a channel this key is not permitted to use. A channel the key *is* permitted but which is currently disabled is not a 403 — it comes back as a per-channel `disabled` result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "429": {
            "description": "Global rate cap reached. Not a 202: a cap has to look like an error so standard retry machinery honours it rather than reading it as success.",
            "headers": {
              "Retry-After": {
                "description": "Seconds until the cap window rolls over.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "result": {
                      "const": "rate_capped"
                    }
                  },
                  "required": ["result"]
                }
              }
            }
          },
          "502": {
            "description": "Every target channel failed permanently.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotifyResponse"
                }
              }
            }
          }
        }
      }
    },
    "/v1/recent": {
      "get": {
        "operationId": "recent",
        "summary": "Recent deliveries, newest first.",
        "description": "A read of the hub's delivery log: what came in, which channel it went to and how it ended up. One item per notification *per channel* — a fan-out to two channels is two items sharing a `requestId`.\n\nOutcomes are reported as settled as the log knows them, so a notification that answered `202 queued` appears here with whatever its delivery eventually was.\n\n**Admin-scoped, and holding a valid key is not enough.** The log carries every caller's titles and caller ids, so reading it takes a key granted `recent = true` in `keys.toml` — or, from a browser, an operator identity this hub's admin allowlist permits. A key that can only send gets a `403`. Every read, refused or served, is recorded in the log it reads.\n\n**To poll:** send `since` set to the newest `ts` you already hold, and check `truncated` — when it is true the answer was cut to `limit` and the *oldest* matching rows were dropped, so raise `limit` and ask again before advancing your cursor. An item resurfaces if its outcome settles after you last saw it, keeping its original `ts` and gaining a `settledAt`.\n\nThe read covers the tail of the log, not its whole history; older entries stay in the file for `grep`. Unknown query parameters are rejected rather than ignored, so a misspelled filter cannot silently return everything.",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "How many items to return, newest first. Out-of-range values are a 400 rather than clamped: an answer silently cut to the maximum is indistinguishable from the log holding only that many.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 500,
              "default": 50
            }
          },
          {
            "name": "since",
            "in": "query",
            "description": "RFC3339 timestamp. Keeps only items that have *changed* since this instant — accepted after it, or settled after it. Pass the `ts` of the newest item you already hold: timestamps are published at millisecond precision and compared at the same precision, so the row you name is excluded rather than returned again on every poll.",
            "schema": {
              "type": "string",
              "format": "date-time"
            }
          },
          {
            "name": "channel",
            "in": "query",
            "description": "Keep only this channel's deliveries. Not checked against the configured channels: a channel deleted this morning still has history worth reading.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The matching deliveries. An empty log is a 200 with `items: []`, never a 404.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecentResponse"
                },
                "example": {
                  "items": [
                    {
                      "ts": "2026-08-08T09:31:02.418Z",
                      "settledAt": "",
                      "requestId": "r_cc519c9a3ee79f270c4e9823cd56f2f3",
                      "caller": "cron",
                      "channel": "ntfy",
                      "title": "Backup failed",
                      "priority": "high",
                      "outcome": "ok"
                    },
                    {
                      "ts": "2026-08-08T09:31:02.418Z",
                      "settledAt": "2026-08-08T09:33:44.902Z",
                      "requestId": "r_cc519c9a3ee79f270c4e9823cd56f2f3",
                      "caller": "cron",
                      "channel": "email",
                      "title": "Backup failed",
                      "priority": "high",
                      "outcome": "failed: smtp: 550 mailbox unavailable"
                    }
                  ],
                  "count": 2,
                  "truncated": false
                }
              }
            }
          },
          "400": {
            "description": "An unknown query parameter, an unparseable `since`, or a `limit` outside 1–500.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "401": {
            "description": "Missing or unknown bearer key, and no permitted admin identity on the request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "403": {
            "description": "The key is valid but not granted delivery-log access. Sending and reading are separate permissions on purpose: a send key that leaks must not also expose every other caller's notifications.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          },
          "500": {
            "description": "The delivery log could not be read.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/health": {
      "get": {
        "operationId": "health",
        "summary": "Liveness.",
        "description": "200 if the process is serving. Liveness only — no outbound probes and no channel state, so a healthy response says nothing about whether deliveries are succeeding.",
        "security": [],
        "responses": {
          "200": {
            "description": "The process is up.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                },
                "example": {
                  "status": "ok",
                  "version": "0.1.0",
                  "uptime": "3h14m0s"
                }
              }
            }
          }
        }
      }
    },
    "/openapi.json": {
      "get": {
        "operationId": "openapi",
        "summary": "This document.",
        "description": "Unauthenticated by design: it exposes the API's shape, which is not a secret, so an agent can be pointed at the base URL and discover the contract before it holds a key. The `channels` enum and the server URL reflect this deployment.",
        "security": [],
        "responses": {
          "200": {
            "description": "The OpenAPI document.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            }
          }
        }
      }
    },
    "/docs": {
      "get": {
        "operationId": "docs",
        "summary": "Browsable API reference.",
        "description": "This document rendered as a page, with a panel for firing a real request against this hub. Generated from the spec on every load rather than maintained beside it, so it cannot describe an endpoint that isn't here.",
        "security": [],
        "responses": {
          "200": {
            "description": "The reference page.",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/llms.txt": {
      "get": {
        "operationId": "llmsTxt",
        "summary": "This contract, condensed, as markdown.",
        "description": "The llms.txt convention (llmstxt.org): what this hub is and how to call it, written for an agent that was handed the host and nothing else. Unauthenticated, and a strict subset of this document — read the spec instead if you can parse it.",
        "security": [],
        "responses": {
          "200": {
            "description": "Markdown, served as text/plain so it renders wherever it is fetched.",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/": {
      "get": {
        "operationId": "index",
        "summary": "Human-readable landing page.",
        "description": "A short HTML page for a person who opened the base URL: what this is, one copy-pasteable request, and the response contract. Also served at `/index.html`. It documents the API and never the deployment behind it, so it names no channel, caller or ops endpoint.",
        "security": [],
        "responses": {
          "200": {
            "description": "The landing page.",
            "content": {
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "description": "A key issued by the operator, sent as `Authorization: Bearer <key>`."
      }
    },
    "schemas": {
      "NotifyRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": ["title", "message"],
        "properties": {
          "title": {
            "type": "string",
            "minLength": 1,
            "x-max-bytes": 256,
            "description": "Required. Max 256 bytes after sanitisation. All control characters are stripped — titles can land in HTTP headers and email subjects."
          },
          "message": {
            "type": "string",
            "minLength": 1,
            "x-max-bytes": 4096,
            "description": "Required. Max 4096 bytes after sanitisation. Newlines and tabs survive (multi-line log excerpts are content); other control characters are stripped. Adapters truncate further to fit the channel. Required even alongside `html` — it is the plain-text alternative, and the only thing a text-only channel can show."
          },
          "html": {
            "type": "string",
            "x-max-bytes": 131072,
            "description": "Optional rich body for channels that can render one — email today, and ignored by every text-only channel. Send a fragment (`<p>done</p>`) to get it wrapped in hubbub's styled mail shell, or a whole document (starting `<!doctype` or `<html`) to own the markup completely. Passed through as written: control characters are stripped and nothing else is, so treat the ability to post here as the ability to put arbitrary markup in the operator's inbox."
          },
          "priority": {
            "type": "string",
            "enum": ["low", "default", "high", "urgent"],
            "default": "default",
            "description": "Display only — never changes which channels fire. Omitted or empty means `default`; parsing is case-insensitive."
          },
          "tags": {
            "type": "array",
            "maxItems": 16,
            "items": {
              "type": "string",
              "x-max-bytes": 64
            },
            "description": "Short labels passed to adapters. Control characters are stripped and tags left empty are dropped."
          },
          "channels": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string"
            },
            "description": "Optional subset of the key's permitted channels. Omit it to use the key's full set. An explicit empty array is rejected with 400 rather than treated as absent. The enum on `items` lists the channel ids this deployment has configured; a key still only reaches the ones it is permitted."
          }
        }
      },
      "NotifyResponse": {
        "type": "object",
        "required": ["result", "requestId", "channels"],
        "properties": {
          "result": {
            "type": "string",
            "enum": ["delivered", "queued", "partial", "failed"],
            "description": "The verdict, repeating what the status code already said. Branch on the status code first and this second — never infer either from the channel map."
          },
          "requestId": {
            "type": "string",
            "description": "Correlation id, also written to the hub's delivery log. Quote it when reporting a problem, and use it to find the settled outcome of anything reported as `queued`."
          },
          "channels": {
            "type": "object",
            "description": "Per-channel outcome, keyed by channel id.",
            "additionalProperties": {
              "type": "string",
              "description": "`ok`, `queued`, `disabled`, or a `failed: …` / `dropped: …` reason."
            }
          }
        }
      },
      "RecentResponse": {
        "type": "object",
        "required": ["items", "count", "truncated"],
        "properties": {
          "items": {
            "type": "array",
            "description": "Deliveries, newest first. Always present, `[]` when nothing matched.",
            "items": {
              "$ref": "#/components/schemas/RecentItem"
            }
          },
          "count": {
            "type": "integer",
            "description": "How many items this response carries — the length of `items`, not the size of the log."
          },
          "truncated": {
            "type": "boolean",
            "description": "True when more rows matched than `limit` allowed. The rows dropped are the *oldest* of the match, so a caller advancing a `since` cursor past this answer would skip them for good: raise `limit` and repeat the request first."
          }
        }
      },
      "RecentItem": {
        "type": "object",
        "required": ["ts", "settledAt", "requestId", "caller", "channel", "title", "priority", "outcome"],
        "properties": {
          "ts": {
            "type": "string",
            "format": "date-time",
            "description": "When the notification was accepted, RFC3339 in UTC at millisecond precision — not when a retry finally settled it. A message that queued at 09:00 and delivered at 11:00 still sorts under 09:00, because that is when it was news. This is the value to pass back as `since`."
          },
          "settledAt": {
            "type": "string",
            "format": "date-time",
            "description": "When a later delivery attempt fixed this outcome, or `\"\"` if the outcome was already final when the notification was accepted. `since` filters on this where it is set, so an outcome settled hours after the fact still reaches a caller whose cursor has moved past `ts`."
          },
          "requestId": {
            "type": "string",
            "description": "The id the sending call returned. Shared by every channel of one fan-out, so it groups the items belonging to a single notification."
          },
          "caller": {
            "type": "string",
            "description": "The caller id from `keys.toml` that sent it — the caller, never the key."
          },
          "channel": {
            "type": "string",
            "description": "The channel id this item is the outcome for."
          },
          "title": {
            "type": "string",
            "description": "The sanitised title as delivered. Empty when the notification's accept line has already scrolled out of the read window and only its settled outcome remains."
          },
          "priority": {
            "type": "string",
            "description": "The priority it was sent with, or empty where the accept line is no longer in the window. Display only, exactly as on the way in."
          },
          "outcome": {
            "type": "string",
            "description": "`ok`, `queued`, `disabled`, or a `failed: …` / `dropped: …` reason — the same vocabulary the send response uses per channel. `queued` here means still in flight at read time, not a promise about the past."
          }
        }
      },
      "Health": {
        "type": "object",
        "required": ["status", "version", "uptime"],
        "properties": {
          "status": {
            "const": "ok"
          },
          "version": {
            "type": "string"
          },
          "uptime": {
            "type": "string",
            "description": "Go duration string, e.g. \"3h14m0s\"."
          }
        }
      },
      "Error": {
        "type": "object",
        "required": ["error"],
        "properties": {
          "error": {
            "type": "string",
            "description": "Human-readable reason. Intended for a log line or an operator, not for programmatic branching — branch on the status code."
          }
        }
      }
    }
  }
}
